.. _sfc_climo_gen_yaml:

sfc_climo_gen YAML
==================

The structured YAML to run :sfc-climo-gen:`sfc_climo_gen<>` is described below. It is validated by JSON Schema.

Consider the example ``config.yaml`` file:

.. highlight:: yaml
.. literalinclude:: ../../../shared/sfc_climo_gen.yaml

asdf
