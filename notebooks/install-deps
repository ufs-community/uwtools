# NB: Update ../binder/environment.yml with changes to runtime packages.

packages=(
  "freezegun=1.5.*" # runtime
  "jupyterlab=4.6.*"
  "testbook=0.4.*"
  "tree=2.2.*" # runtime
)
conda install -q -y --repodata-fn repodata.json ${packages[@]}
