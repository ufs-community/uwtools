ecflow:
  suite_test:
    vars:
      SUITE_VAR: "value"
    family_prep:
      family_inner:
        task_inner:
          script:
            execution:
              executable: "echo hello from inner family"
      task_setup:
        trigger: "1==1"
        script:
          execution:
            executable: "echo hello from ecFlow"
    task_run:
      trigger: "/test/prep/setup == complete"
      script:
        execution:
          executable: "echo running main task"
